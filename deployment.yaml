apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert22
spec:
  selector:
    matchLabels:
      app: alert22
  template:
    metadata:
      labels:
        app: alert22
    spec:
      containers:
      - name: alert22
        image: harishoo7/alert
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        
